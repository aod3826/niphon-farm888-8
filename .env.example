# Gemini API Key (Server-side)
GEMINI_API_KEY=

# Supabase Configuration (Optional for cloud persistence; fallback to built-in relational store if not set)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
